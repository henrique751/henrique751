// Configuração pública do site. Nunca coloque chaves secretas neste arquivo.
window.TT_CONFIG = {
  // URL HTTPS do backend/serverless que executa openai-worker.js.
  // Enquanto permanecer vazio, o Assistente usa o catálogo local completo.
  aiEndpoint: ''
};
